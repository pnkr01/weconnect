import 'package:flutter/material.dart';

class CompanyCreation extends StatelessWidget {
  const CompanyCreation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Create a company",),
        centerTitle: true,
      ),
      body: SingleChildScrollView(
        child: Column(
          children: [
            
          ],
        ),
      ),
    );
  }
}